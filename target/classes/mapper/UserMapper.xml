<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leetcode.mapper.UserMapper">

    <insert id="add">
        insert into user (username, password, email) value (#{username}, #{password}, #{email})
    </insert>

    <update id="update">
        update t_user
        set uname=#{uname},
            pwd=#{pwd},
            age=#{age}
        where dbid = #{id}
    </update>

    <delete id="delete">
        delete
        from t_user
        where dbid = #{id}
    </delete>

    <select id="getById" resultMap="userMap">
        select *
        from t_user
        where dbid = #{id}
    </select>

    <select id="getByPhone" resultMap="userMap">
        select *
        from user
        where phone = #{phone}
    </select>

    <select id="getByUsername" resultMap="userMap">
        select *
        from user
        where username = #{username}
    </select>

    <select id="getByEmail" resultMap="userMap">
        select *
        from user
        where email = #{email}
    </select>

    <resultMap id="userMap" type="com.leetcode.vo.User">
        <id column="userId" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="image" property="image"/>
    </resultMap>

</mapper>